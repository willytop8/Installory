# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode / SPM build output
.build/
build/
.swiftpm/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/

# Generated Xcode project (source of truth is project.yml)
Installory.xcodeproj/
*.xcuserstate

# Swift Package Manager
# Package.resolved is intentionally committed (not ignored) to pin the exact
# GRDB version for reproducible builds. Xcode/SPM generates it on the next build.

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs and tmp files
*.log
*.tmp

# Personal/project-tool artifacts
.claude/settings.local.json

# AI-agent orchestration scaffolding (implementation specs, audits, handoff notes).
# Working material, not user-facing documentation — this repo stays public-facing.
handoff/

# Local Netlify folder
.netlify

# Marketing site — lives in its own git repo at website/, deployed via Netlify CLI.
# Kept out of the app repo so this one stays purely about the app.
website/
netlify.toml
